const form_focus = window.document.querySelector('.form-input') 

	form_focus.addEventListener('mouseenter' , (event) => {
		if(window.document.form.elements[0]) document.querySelector('.container-input').focus()

});
		